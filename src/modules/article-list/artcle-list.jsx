import React from "react";
import styled from "styled-components";

const Title = styled.h1`
  color: white;
`;

const ArticleList = () => {
  return <Title>Article list</Title>;
};

export default ArticleList;
